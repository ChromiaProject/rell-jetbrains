# Rell Intellij BNF Grammar Generator

In order to generate the grammar for the Rell language, you need to either
1. Run the `run` gradle task. `./gradlew run`
2. or run the `BnfGrammar.kt` file from IDE

It'll print result in the console and can be redirected/saved to a file.