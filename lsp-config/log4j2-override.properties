# Replaces the log4j2 config bundled inside rell-toolbox-language-server-*.jar, which wires a
# Sentry appender into the root logger at ERROR level. That appender uploads every error the
# language server logs, with no user consent and no opt-out, and the messages carry absolute
# file paths. Crash reporting in this plugin is opt-in only, through the IDE error dialog
# (see SentryReportSubmitter), so the language server must not report on its own.
#
# This is a verbatim copy of the bundled config minus the Sentry appender. Keep it in sync when
# the upstream config changes; drop it once the language server no longer bundles the appender.
status=warn
name=LanguageServerLogger

# RollingFile Appender
appender.rolling.type=RollingFile
appender.rolling.name=RollingFile
appender.rolling.fileName=${sys:java.io.tmpdir}/rell-language-server/lsp.log
appender.rolling.filePattern=${sys:java.io.tmpdir}/rell-language-server/lsp-%i.log
appender.rolling.layout.type=PatternLayout
appender.rolling.layout.pattern=%-5level %d{yyyy-MM-dd HH:mm:ss.SSS} [%t] %c{1} - %msg%n
appender.rolling.filter.threshold.type=ThresholdFilter
appender.rolling.filter.threshold.level=debug
appender.rolling.policies.type=Policies
appender.rolling.policies.size.type=SizeBasedTriggeringPolicy
appender.rolling.policies.size.size=10MB
appender.rolling.strategy.type=DefaultRolloverStrategy
appender.rolling.strategy.max=10
# Console appender configuration
appender.console.type=Console
appender.console.name=ConsoleLogger
appender.console.layout.type=PatternLayout
appender.console.layout.pattern=%-5level %d{yyyy-MM-dd HH:mm:ss.SSS} [%t] %c{1} - %msg%n
appender.console.filter.threshold.type=ThresholdFilter
appender.console.filter.threshold.level=info
# Root logger level
rootLogger.level=info
# Root logger referring to console appender
rootLogger.appenderRefs=console
rootLogger.appenderRef.console.ref=ConsoleLogger
